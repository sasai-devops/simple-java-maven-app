pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                echo "Test"
                //sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
